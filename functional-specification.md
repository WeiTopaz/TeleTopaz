# 功能規格書（Functional Specification）

**文件目的**：以純自然語言完整描述本系統的功能邏輯、輸入輸出、狀態管理、安全機制及邊界條件，作為淨室設計（Clean Room Design）之唯一重寫依據。本文件不包含、也不應參照任何原始程式碼。

**系統概述**：一個「即時通訊機器人」系統，連接即時通訊平台（Telegram）與 AI 程式碼助理服務（GitHub Copilot SDK），允許授權使用者透過行動裝置上的聊天介面，向 AI 助理提交自然語言提示詞，AI 助理在指定的本機工作目錄中執行任務（包含工具呼叫），並將結果串流回聊天介面。

---

## 第一章：系統架構概觀

### 1.1 系統角色

系統中共有三個主要角色：

1. **即時通訊平台**：提供聊天介面、訊息收發、按鈕互動（Inline Keyboard / Callback Query）、表情回應（Message Reaction）及檔案傳輸等能力。系統透過長輪詢（Long Polling）持續監聽來自平台的更新事件。
2. **AI 助理服務**：一個透過 SDK 連接的外部 AI 代理。系統需啟動一個客戶端（Client），並在該客戶端上建立工作階段（Session）。工作階段綁定特定的 AI 模型與工作目錄，可接收提示詞、發出事件（包含助理回覆、工具執行通知、工作階段閒置通知等）。
3. **機器人服務程序**：本系統的核心，負責橋接即時通訊平台與 AI 助理服務。它管理使用者認證、對話狀態、訊息佇列、安全護欄，並在兩者之間轉譯與路由訊息。

### 1.2 整體運作流程

1. 系統啟動時，從作業系統安全儲存（OS Keychain）載入必要的機密設定。
2. 初始化即時通訊平台客戶端，開始長輪詢。
3. 清除啟動前遺留的離線訊息。
4. 向指定的擁有者發送歡迎訊息。
5. 使用者透過指令選擇工作目錄並建立 AI 工作階段。
6. 使用者提交自然語言提示詞；系統經過安全護欄檢查後轉發給 AI 助理。
7. AI 助理的回覆事件被逐一處理並轉發至聊天介面。
8. 工作階段閒置後，自動處理佇列中的下一個提示詞。
9. 收到關閉指令時，優雅地銷毀所有工作階段並退出。

---

## 第二章：設定與機密管理

### 2.1 機密載入

系統的所有敏感設定值均儲存在作業系統的安全鑰匙圈中（如 macOS Keychain），由一個通用的「機密存取介面」讀取。系統絕不從環境變數或設定檔直接讀取機密。

**必要設定值**：
- 即時通訊平台的 Bot 令牌（Bot Token）
- 擁有者的聊天識別碼（Owner Chat ID）——用於限制只有此聊天 ID 可以使用機器人
- 擁有者的使用者識別碼（Owner User ID）——用於雙重驗證使用者身份

**選用設定值**：
- 工作目錄模式字串（Directory Patterns）——可透過鑰匙圈覆寫檔案中的設定
- 即時通訊平台的 TLS 憑證指紋（Certificate Fingerprints）——用於憑證鎖定

**邊界條件**：
- 若任何必要設定值缺失或為空，系統應立即報錯並終止啟動。
- 若機密存取介面不可用（例如 keytar 無法載入），系統應報錯並終止。

### 2.2 機密設定腳本

系統提供一個互動式的設定腳本，允許使用者透過命令列互動提示輸入機密值，或透過環境變數傳入（適用於自動化情境），然後將值寫入作業系統鑰匙圈。

**邏輯流程**：
1. 對每個設定項目：先嘗試從環境變數取得；若無，則透過互動式提示要求使用者輸入。
2. 必填欄位若為空值則中止程序。
3. 選填欄位允許為空（不儲存）。
4. 所有值經修剪空白後寫入鑰匙圈。

---

## 第三章：工作目錄管理

### 3.1 目錄模式（Directory Patterns）

系統透過「Glob 模式」定義使用者可以選擇的工作目錄。模式來源有兩個，依優先級：

1. **環境變數**（或鑰匙圈中的覆寫值）：一個逗號分隔的 Glob 模式字串。
2. **設定檔**：專案根目錄下的一個 JSON 陣列檔案，每個元素是一個 Glob 模式字串。

**邊界條件**：
- 若環境變數存在且非空，則使用它，忽略設定檔。
- 若設定檔不存在或解析失敗，回傳空陣列。
- 設定檔內容必須是字串陣列，否則回傳空陣列。

### 3.2 模式展開（Pattern Expansion）

將 Glob 模式陣列展開為唯一的絕對目錄路徑列表。

**邏輯流程**：
1. 對每個模式，將反斜線統一轉換為正斜線（跨平台兼容）。
2. 使用 Glob 匹配引擎展開模式，不匹配隱藏檔案。
3. 對每個匹配結果：移除尾端斜線，檢查路徑是否為目錄（透過檔案系統狀態查詢）。
4. 若為目錄，將其轉換為絕對路徑並加入結果集合（使用 Set 去重）。
5. 忽略任何不存在或非目錄的匹配。
6. 最終回傳排序後的唯一絕對路徑陣列。

**邊界條件**：
- 若模式陣列為空，結果為空。
- 若所有模式均無匹配，結果為空。
- 檔案系統查詢失敗（例如權限不足）的項目靜默忽略。

### 3.3 目錄安全驗證

任何使用者選擇的目錄在被用於建立工作階段之前，必須通過「允許目錄」驗證。

**邏輯**：
1. 取得當前所有允許的目錄列表（經展開後）。
2. 將目標目錄與所有允許目錄都進行路徑正規化（resolve + normalize）。
3. 在 Windows 平台上，正規化後轉為全小寫以進行不區分大小寫的比較。
4. 若目標目錄存在於允許列表中，則驗證通過。

---

## 第四章：使用者認證與授權

### 4.1 擁有者驗證

系統採用「雙重 ID 驗證」：一條訊息的發送者必須同時滿足「聊天 ID 等於擁有者聊天 ID」且「使用者 ID 等於擁有者使用者 ID」才被視為授權的擁有者。

**邊界條件**：
- 若訊息缺少使用者 ID（例如來自頻道的訊息），驗證失敗。
- 非擁有者的任何操作（指令、訊息、按鈕點擊）都會收到一條客氣的拒絕訊息。

### 4.2 單一使用者限制

在任何時刻，系統只允許一個活躍的 AI 工作階段。若一個使用者已有活躍工作階段，其他使用者嘗試建立工作階段時將被拒絕。

---

## 第五章：安全護欄（Guardrails）

### 5.1 護欄策略載入

系統從專案根目錄或程式入口目錄尋找一個名為 `guardrails.json` 的策略檔案。

**策略檔案結構**：
- 版本號（整數）
- 提示詞最大長度（選用，整數）
- 拒絕規則陣列（Deny Rules）
- 允許規則陣列（Allow Rules）

每條規則包含：
- 唯一識別碼
- 描述文字（選用）
- 包含比對字串陣列（contains，不區分大小寫比對）
- 正則表達式陣列（regex，不區分大小寫比對）

**邊界條件**：
- 若找不到策略檔案，使用預設策略（最大長度 4000，無自訂規則）。
- 若策略檔案解析失敗或缺少版本號，回退到預設策略。

### 5.2 內建安全規則

除了使用者自訂的策略規則外，系統內建以下不可關閉的安全規則（硬編碼於系統中）：

1. **私鑰偵測**：偵測 PEM 格式的私鑰區塊標頭（RSA、EC、OPENSSH 等格式）。
2. **已知令牌模式偵測**：偵測常見的 API 令牌格式，包括以特定前綴開頭的長英數字串（如 `sk-`、`ghp_`、`ghs_` 等格式，以及 Telegram Bot Token 的數字冒號格式）。
3. **提示注入偵測**：偵測嘗試覆寫系統指令的措辭（如「忽略前述指示」、「覆寫系統」等組合）。
4. **機密洩漏偵測**：偵測明確要求揭露機密的措辭（如「顯示密碼」、「洩漏令牌」等組合）。
5. **敏感路徑存取偵測**：偵測嘗試存取系統敏感路徑的請求（如 `/etc/passwd`、`.ssh/`、`.aws/` 等）。

### 5.3 提示詞安全評估

對每個使用者提交的提示詞，依以下順序進行評估，一旦命中即停止：

1. **長度檢查**：若提示詞長度超過策略中設定的最大值，拒絕。
2. **內建規則檢查**：依序檢查所有內建安全規則。
3. **策略拒絕規則**：依序檢查 `denyRules` 中的每條規則，支援 `contains`（不區分大小寫子字串比對）與 `regex`（不區分大小寫正則匹配）。
4. **策略允許規則**：依序檢查 `allowRules`（邏輯同上，但結果為「允許」）。
5. **預設允許**：若無規則命中，預設允許。

**回傳值**：一個決策物件，包含是否允許、命中的規則 ID、來源（內建 / 策略 / 長度檢查）、以及拒絕理由。

**邊界條件**：
- 空提示詞或 null 值被視為空字串處理。
- 策略中的無效正則表達式被靜默忽略，不影響其他規則。
- 工具輸出也會經過安全評估，以偵測間接提示注入（Indirect Prompt Injection）。

### 5.4 敏感資訊遮蔽（Redaction）

所有對外顯示的文字（日誌、使用者訊息、錯誤堆疊）都經過敏感資訊遮蔽處理。

**遮蔽目標與模式**：
1. PEM 私鑰區塊（BEGIN...END 完整區塊）
2. 環境變數賦值模式（`大寫變數名_TOKEN/_KEY/_SECRET/_PASSWORD = 值`）
3. OpenAI 格式令牌（`sk-` 前綴 + 16 位以上英數字）
4. Telegram Bot Token 格式（6+ 位數字 + 冒號 + 20+ 位英數字）
5. GitHub 令牌格式（`ghp_`/`gho_`/`ghu_`/`ghs_`/`ghr_` + 20+ 位英數字）
6. AWS Access Key 格式（`AKIA` + 16 位大寫英數字）

所有命中的內容替換為固定的遮蔽文字 `[REDACTED]`。

### 5.5 集中式日誌

系統使用一個集中式的日誌器，它包裹標準輸出/錯誤輸出，並在輸出前自動對所有字串參數以及錯誤物件的訊息與堆疊追蹤進行敏感資訊遮蔽。

---

## 第六章：TLS 憑證鎖定

### 6.1 憑證指紋管理

系統支援對即時通訊平台 API 伺服器的 TLS 憑證進行鎖定（Certificate Pinning），以防範中間人攻擊。

**指紋處理邏輯**：
1. 從設定中取得逗號分隔的指紋列表字串。
2. 每個指紋經過「正規化」處理：移除所有非十六進位字元，轉為大寫。
3. 過濾掉空的指紋。

### 6.2 伺服器身份驗證函式

系統建立一個自訂的「伺服器身份驗證函式」，在 TLS 握手時執行以下檢查：

1. **主機名稱比對**：連線的主機名稱必須與預期的 API 主機完全一致。
2. **標準身份驗證**：呼叫系統內建的 TLS 伺服器身份驗證。
3. **指紋比對**（僅在有設定鎖定指紋時）：
   - 取得伺服器憑證的 SHA-256 指紋（優先使用憑證物件自帶的 `fingerprint256` 屬性；若不可用，則從原始憑證資料計算 SHA-256 雜湊）。
   - 若無法取得指紋，驗證失敗。
   - 若指紋不在鎖定列表中，驗證失敗。

**邊界條件**：
- 若未設定任何鎖定指紋，跳過指紋比對步驟（僅做標準驗證）。
- 主機名稱不匹配時立即返回錯誤，不繼續後續檢查。

---

## 第七章：對話狀態管理

### 7.1 對話狀態結構

系統為每個聊天 ID 維護一個獨立的對話狀態物件。

**狀態欄位**：
- AI 客戶端實例（可為空）
- AI 工作階段實例（可為空）
- 當前工作目錄路徑（可為空）
- 當前選用的 AI 模型名稱
- 忙碌旗標（布林值：是否正在處理提示詞）
- 提示詞佇列（字串陣列）
- 重設中旗標（布林值：是否正在重設工作階段）
- 附加圖片陣列（Data URL 字串陣列）
- 工作階段表情符號（用於在訊息中識別不同工作階段）
- 當前正在處理的提示詞文字
- 工具執行起始訊息對應表（用於追蹤工具呼叫的進度）
- 等待最終回覆旗標
- 待發送完成通知旗標
- 事件佇列與處理中旗標（用於序列化事件處理）
- 原始使用者訊息 ID（用於回覆引用）
- 處理中佔位訊息 ID（用於就地編輯回覆）
- 處理逾時計時器（20 秒後更新佔位訊息）
- 是否已收到助理回覆旗標
- 上次助理回覆的雜湊值（用於去重）
- 輪次計數器（已執行的對話輪次數）
- 最近使用模型列表（最多 2 個，不含當前模型）
- 可用目錄快取
- 是否已注入人格提示（布林值）

### 7.2 工作階段表情符號

系統維護一個表情符號池。每當建立新的工作階段時，從池中選取一個尚未被其他活躍工作階段使用的表情符號，作為該工作階段的視覺標識。

**邏輯**：
1. 收集所有活躍工作階段已使用的表情符號。
2. 從池中依序找第一個未被使用的表情符號。
3. 若全部已用，隨機選取一個。

### 7.3 最近使用模型列表

當使用者切換 AI 模型時，系統記錄最近使用過的模型（最多 2 個），以便在 UI 上提供快速切換按鈕。

**邏輯**：
1. 將舊模型加入列表最前端。
2. 移除重複項。
3. 保留前 2 個。

---

## 第八章：AI 工作階段生命週期

### 8.1 建立工作階段

**前置條件**：
- 無其他使用者的活躍工作階段（或為同一使用者）。
- 指定的目錄通過允許目錄驗證。

**流程**：
1. 若對話狀態不存在，建立一個全新的狀態物件並分配表情符號。
2. 若已有工作階段，先銷毀舊的工作階段並停止舊的客戶端。
3. 切換至指定的工作目錄。
4. 建立新的 AI 客戶端並啟動。
5. 載入「人格提示」（Persona Prompt），作為工作階段的系統訊息。
6. 決定使用的模型：優先使用指定模型，其次為對話狀態中的模型，最後為預設模型。
7. 使用指定的模型、系統訊息、工作目錄建立新的工作階段。工作階段設定包含三個生命週期鉤子：
   - **工具使用前鉤子**：記錄工具名稱，預設允許所有工具執行。
   - **工具使用後鉤子**：記錄工具名稱。
   - **錯誤發生鉤子**：記錄錯誤上下文，預設策略為重試。
8. 註冊事件處理器（所有工作階段事件會被序列化排入佇列）。
9. 更新對話狀態的所有欄位，重設佇列與旗標。
10. 發送建立成功通知與當前環境摘要（專案名稱與模型名稱）。

**錯誤處理**：
- 銷毀舊工作階段/停止舊客戶端時，若遇到「連線已釋放（Connection Disposed）」錯誤，靜默忽略（這是正常的生命週期行為）。
- 切換目錄失敗時拋出包含目錄路徑的錯誤。
- 建立工作階段失敗時，停止客戶端，釋放活躍使用者鎖定，並向上層拋出錯誤。

### 8.2 人格提示載入

系統從工作目錄中讀取特定檔案，組合成一段上下文提示，注入 AI 工作階段作為系統訊息。

**載入的檔案**：
1. `MEMORY.md`——長期記憶檔案
2. `AGENTS.md`——代理行為指引
3. 今日日記檔案（`日記/YYYY-MM-DD.md`）

**組合邏輯**：
1. 首先加入固定的環境資訊：AI 身份描述、當前工作目錄路徑。
2. 依序嘗試讀取上述檔案。若存在，以標題分隔加入提示文字。
3. 若任何檔案讀取失敗，靜默忽略。
4. 若完全沒有找到任何檔案，附加一段預設的簡短行為指引。
5. 最後加入結束語，提醒 AI 使用繁體中文回覆。

### 8.3 銷毀工作階段（重設）

**流程**：
1. 清空提示詞佇列。
2. 設定重設旗標。
3. 中止（Abort）當前工作階段。
4. 以相同的目錄和模型重新建立工作階段。
5. 清除重設旗標，重設輪次計數器。

### 8.4 優雅關閉

收到終止訊號（SIGINT）時：
1. 停止即時通訊平台的長輪詢。
2. 對所有活躍的對話狀態：平行地銷毀工作階段（6 秒逾時）與停止客戶端（4 秒逾時）。
3. 所有清理操作有 12 秒的整體逾時。
4. 清理完成後退出程序。
5. 銷毀/停止過程中的「連線已釋放」錯誤靜默忽略。

---

## 第九章：事件處理系統

### 9.1 事件佇列化

AI 工作階段發出的所有事件先進入一個佇列，再逐一處理。這確保事件按順序執行，避免競爭條件（例如工具結果訊息在「閒置」事件之前完成發送）。

**邏輯**：
1. 事件到達時推入對話狀態的事件佇列。
2. 若未在處理中，啟動處理循環。
3. 處理循環依序取出事件並呼叫處理函式，直到佇列清空。
4. 任何處理錯誤被捕獲並記錄，不中斷後續事件處理。

### 9.2 助理回覆事件（assistant.message）

**行為**：
1. 從事件資料中提取文字內容（嘗試多個屬性名稱與巢狀結構）。
2. 若內容非空，計算內容的雜湊值。若與上次收到的回覆雜湊相同，靜默忽略（去重機制，防止重複訊息）。
3. 更新上次回覆雜湊。
4. **就地編輯或新發送**：
   - 若存在「處理中」佔位訊息且回覆長度不超過 3500 字元，直接編輯該佔位訊息為回覆內容（節省訊息數量）。
   - 否則，發送新訊息，並將佔位訊息編輯為「✅完成」。
5. 清除「等待最終回覆」旗標，標記已收到助理回覆。
6. 取消處理逾時計時器。
7. 若存在「待發送完成通知」且佇列為空，發送完成狀態報告。

### 9.3 助理串流差量事件（assistant.message_delta）

**行為**：
- 此事件被靜默忽略。系統僅處理最終的完整回覆（assistant.message），以避免重複發送中間片段。

### 9.4 工具執行開始事件（tool.execution_start）

**行為**：
1. 提取工具名稱、工具呼叫 ID、工具參數（嘗試多個可能的屬性名稱）。
2. 產生兩個唯一的儲存金鑰（格式：Base36 時間戳 + 12 位隨機十六進位字串），分別用於參數與結果。
3. 對參數文字進行敏感資訊遮蔽，並截取前 150 字元作為摘要。
4. 發送一則包含工具名稱與參數摘要的通知訊息，附帶「查看參數」與「查看結果」互動按鈕。
5. 將參數儲存至參數儲存區（供按鈕查詢）。
6. 將訊息 ID 與相關資訊儲存至工具起始訊息對應表（供完成事件查找）。

### 9.5 工具執行完成事件（tool.execution_complete）

**行為**：
1. 透過工具呼叫 ID 或父事件 ID 在對應表中查找起始訊息。
2. 提取結果或錯誤訊息。若結果為 JSON 字串則嘗試美化格式。
3. **對工具輸出進行安全護欄檢查**（偵測間接提示注入）。
4. 對結果文字進行敏感資訊遮蔽，截取摘要。
5. 更新原始「工具執行中」訊息的內容，加入成功/失敗狀態與結果摘要。
6. 將完整結果儲存至結果儲存區（供「查看結果」按鈕使用）。
7. 對原始訊息添加表情回應（成功：✅，失敗：❌）。
8. 從對應表中移除該工具呼叫條目。

### 9.6 工作階段閒置事件（session.idle）

**行為**：
1. 清除忙碌旗標與當前提示詞。取消處理逾時計時器。
2. 若佇列為空：
   - 若正在等待最終回覆，設定「待發送完成通知」旗標（等回覆到了再通知）。
   - 若未收到任何助理回覆，立即發送完成狀態報告。
3. 若佇列非空：
   - 取出佇列首個提示詞。
   - 設定忙碌旗標，遞增輪次計數器。
   - 重設等待回覆、完成通知、已收到回覆、回覆雜湊等旗標。
   - 發送新的「⏳處理中」佔位訊息。
   - 將提示詞發送給 AI 工作階段。
   - 發送失敗時清除忙碌旗標並通知使用者。

---

## 第十章：使用者指令系統

### 10.1 指令列表

| 指令 | 功能描述 |
|------|----------|
| `/help` | 顯示說明與指令列表 |
| `/project` | 列出所有允許的工作區，以互動按鈕形式供選擇 |
| `/model [編號]` | 無參數時列出所有可用模型；有參數時選擇指定模型 |
| `/info` 或 `/i` | 檢視當前工作階段狀態報告 |
| `/new` | 中止當前工作階段並重新建立 |
| `/imgclear` | 清除已暫存的附件圖片 |
| `/bye` | 優雅關閉整個機器人程序 |

所有指令都需通過擁有者驗證。

### 10.2 工作區選擇指令（/project）

**流程**：
1. 取得所有展開後的允許目錄。
2. 若無可用目錄，通知使用者。
3. 建立互動鍵盤，每行最多 2 個按鈕，按鈕文字為目錄的基本名稱（最後一段路徑）。
4. 將可用目錄列表存入對話狀態（供後續按鈕回調使用）。

### 10.3 模型選擇指令（/model）

**不帶參數**：
1. 列出所有可用模型名稱，當前選用的模型以綠色圓點標記。
2. 以互動鍵盤顯示，每行最多 2 個按鈕。

**帶數字參數**：
1. 將數字轉為 0-based 索引（使用者輸入 1-based）。
2. 驗證索引範圍。
3. 更新對話狀態的模型。
4. 將舊模型加入「最近使用」列表。
5. 若工作階段已啟動，以新模型重新建立工作階段。

**邊界條件**：
- 無效索引回傳錯誤訊息。
- 非數字輸入回傳錯誤訊息。

### 10.4 狀態報告（/info）

顯示以下資訊：
- 擁有者的聊天 ID
- 連線狀態（已連線 / 未連線）
- 選用的 AI 模型
- 工作區
- 已執行輪次數
- 是否處理中
- 待辦任務數量（含前 2 項預覽，截取前 40 字元）
- 是否正在重設
- 當前任務（截取前 100 字元）

**附帶互動按鈕**：若已設定工作區，顯示模型切換與重開按鈕；否則顯示選擇工作區與模型切換按鈕。另外，根據「最近使用模型」列表，動態產生快速切換按鈕（帶星號標記）。

### 10.5 關閉指令（/bye）

**特殊處理**：
- 忽略機器人啟動之前的訊息（透過比較訊息時間戳與啟動時間戳），防止重啟後重播舊的關閉命令。
- 觸發與 SIGINT 相同的優雅關閉流程。

---

## 第十一章：互動按鈕系統（Callback Query）

### 11.1 概述

系統大量使用「內嵌式鍵盤」（Inline Keyboard）提供快捷操作。按鈕點擊產生回調查詢，依 `callback_data` 前綴路由到不同的處理邏輯。

### 11.2 按鈕類型

| callback_data 格式 | 功能 |
|---------------------|------|
| `do.project` | 觸發目錄列表顯示 |
| `do.model` | 觸發模型列表顯示 |
| `do.info` | 觸發狀態報告 |
| `do.help` | 觸發歡迎訊息 |
| `do.new` | 觸發工作階段重設 |
| `do.bye` | 觸發機器人關閉 |
| `pick.proj:{索引}` | 選擇特定目錄並啟動工作階段 |
| `pick.eng:{索引}` | 選擇特定模型 |
| `peek.res:{金鑰}` | 顯示工具執行的完整結果 |
| `peek.arg:{金鑰}` | 顯示工具執行的完整參數 |

### 11.3 按鈕回調的通用處理

1. 所有回調都先忽略機器人啟動前的舊訊息。
2. 所有回調都進行擁有者驗證。
3. 回應回調查詢（answer callback query）以消除平台上的載入動畫。

### 11.4 模型快速切換按鈕

狀態報告中除了「切換模型」按鈕外，還會根據「最近使用模型」列表，動態產生最多 2 個快速切換按鈕（顯示模型名稱，帶星號標記）。

---

## 第十二章：訊息處理

### 12.1 圖片附件處理

使用者可以傳送圖片（壓縮照片或原始檔案文件），系統將其下載並以 Base64 Data URL 格式儲存在對話狀態中。

**流程**：
1. 偵測訊息是否包含照片或圖片文件。
2. 若圖片數量已達上限（8 張），拒絕並通知。
3. 透過平台 API 取得檔案資訊。若無法取得路徑或大小超過上限（8 MB），拒絕。
4. 透過自訂的 HTTPS 下載函式（帶 TLS 憑證鎖定）下載檔案。
5. 再次檢查實際下載大小。
6. 若為壓縮照片格式，透過圖片處理引擎重新編碼為 JPEG 格式（自動旋轉、品質 80%、啟用壓縮優化）；若重新編碼失敗，通知使用者並中止。
7. 將最終的 Buffer 轉為 Data URL 格式（`data:MIME;base64,...`）。
8. 壓縮照片使用 `image/jpeg` MIME 類型；文件使用其原始 MIME 類型。
9. 非圖片 MIME 類型的文件會被拒絕。
10. 通知使用者已附加的圖片數量。

**邊界條件**：
- 無對話狀態時自動建立。
- 若訊息僅有圖片無文字，處理完圖片後停止（不送提示詞）。
- 下載 URL 的主機名稱必須與預期的 API 主機一致（防止開放式重定向）。

### 12.2 一般文字訊息處理

**流程**：
1. 忽略無文字內容的訊息。
2. 忽略以 `/` 開頭的訊息（由指令處理器處理）。
3. 若無活躍工作階段或未選擇目錄，提示使用者先選擇目錄。
4. 若工作階段正在重設，拒絕新提示詞。
5. 對提示詞進行安全護欄評估。若被拒絕，顯示拒絕原因與規則 ID。
6. 將附加的圖片（若有）組合到提示詞中。
7. 若工作階段忙碌：
   - 若佇列已滿（上限 15），拒絕。
   - 否則加入佇列並通知使用者。
8. 若工作階段閒置：
   - 設定忙碌旗標、當前提示詞、原始訊息 ID。
   - 遞增輪次計數器。
   - 發送一則「⏳處理中」佔位訊息（截取提示詞前 80 字元作為摘要）。
   - 啟動 20 秒逾時計時器：若逾時仍未收到回覆，更新佔位訊息為「⏳處理中…仍在等待回覆」。
   - 發送提示詞給 AI 工作階段。

### 12.3 圖片附加至提示詞

當提示詞需要附加圖片時，將圖片 Data URL 列表以編號清單的形式附加在提示詞文字末尾。

### 12.4 清除附加圖片

使用者可透過 `/imgclear` 指令清除對話狀態中的所有附件圖片。

---

## 第十三章：訊息格式化與發送

### 13.1 長訊息分割

即時通訊平台對每則訊息有字元上限（4096 字元）。系統自動將長訊息分割成多段發送。

**分割邏輯**：
1. 在每段的結尾處，嘗試在上限的 75% 位置之後找到最近的換行符進行分割。
2. 若找不到適合的換行符，直接在上限處截斷。
3. 只有第一段回覆原始訊息（reply_to_message_id）。

### 13.2 Markdown 格式轉換

系統將標準 Markdown 轉換為平台特定的 MarkdownV2 格式。

**轉換邏輯**：
1. 先將文字以程式碼區塊（三個反引號）分割。程式碼區塊內部只跳脫反斜線和反引號。
2. 非程式碼區塊中，再以行內程式碼（單個反引號）分割。行內程式碼內部只跳脫反斜線和反引號。
3. 一般文字中：
   - 將 Markdown 標題（`#` 開頭）轉為粗體格式。
   - 跳脫所有 MarkdownV2 特殊字元（`_ * [ ] ( ) ~ ` > # + - = | { } . ! \`）。
   - 選擇性還原正確配對的格式化標記：`**粗體**` → 單星號粗體、`__底線__`、`~~刪除線~~` → 單波浪線、`*粗體*`。
   - 斜體底線（`_文字_`）僅在非文字邊界處還原，以避免誤判底線命名的標識符。

### 13.3 訊息發送包裝

系統包裝平台的原始發送訊息函式，加入以下行為：

1. **標頭注入**：在訊息前加入「工作階段表情符號 + 助理名稱 + 工作目錄名稱」的標頭。若訊息已以表情符號開頭（包括符號池中的表情或任何擴展象形文字），不重複加入。
2. **敏感資訊遮蔽**：對完整文字進行遮蔽。
3. **格式轉換**：轉換為 MarkdownV2 格式。
4. **降級回退**：若 MarkdownV2 解析失敗（平台拒絕），移除所有 Markdown 語法標記，以純文字重新發送。

### 13.4 編輯訊息的降級機制

編輯已發送訊息時，使用三層降級策略：
1. 先嘗試以 MarkdownV2 格式編輯。
2. 若失敗，移除格式化標記，以純文字模式編輯（不帶 parse_mode）。
3. 若再失敗，改為發送新訊息。

---

## 第十四章：啟動流程

### 14.1 主程序啟動順序

1. 初始化沙箱模式（若啟用且為 macOS）。
2. 從鑰匙圈載入設定。
3. 解析 TLS 憑證指紋並建立即時通訊平台客戶端（帶自訂伺服器身份驗證）。
4. 記錄啟動時間戳（Unix 秒）。
5. 載入可用模型列表與安全護欄策略。
6. 載入可用目錄列表。
7. 若有可用模型，為擁有者聊天設定預設模型。
8. 記錄可用目錄與預設模型。
9. 清除離線期間累積的更新（迴圈呼叫取得更新 API，每次最多 100 筆，直到無更新為止）。
10. 開始長輪詢（逾時 25 秒）。
11. 查詢並顯示 AI 服務提供者資訊（版本、認證狀態、可用模型列表按提供者分組）。
12. 向擁有者發送歡迎訊息與提供者資訊。
13. 註冊程序終止處理：
    - 首次 SIGINT：啟動優雅關閉流程（4 秒超時後強制退出）。
    - 二次 SIGINT：立即強制退出。
    - 未捕獲的 Promise 拒絕：若正在關閉且為連線釋放錯誤，靜默忽略；否則記錄錯誤。
    - 未捕獲的例外：記錄錯誤。

### 14.2 歡迎訊息

歡迎訊息包含以下資訊：
- 系統標題
- 伺服器當前時間（台灣地區格式）
- 擁有者名稱（透過 platform API 查詢）
- 當前工作區名稱
- 當前 AI 模型名稱
- 所有可用指令列表
- 可用模型資訊（按提供者分組顯示）

附帶四個互動按鈕：專案、模型、狀態、重開。

### 14.3 提供者資訊查詢

**流程**：
1. 建立一個臨時 AI 客戶端。
2. 依序查詢：版本資訊、服務狀態（含協定版本）、認證狀態（含使用者名稱）、可用模型列表。每個查詢獨立捕獲錯誤，不影響其他查詢。
3. 將可用模型按推斷的提供者分組，格式化為階層式列表。
4. 關閉臨時客戶端。
5. 任何查詢失敗時靜默忽略，回傳已取得的部分資訊。

---

## 第十五章：工具函式庫

### 15.1 索引解析

將使用者輸入的 1-based 字串索引轉為 0-based 數字索引，並驗證範圍。

**邊界條件**：
- 輸入為空、非數字、小於 1、大於等於最大值，均回傳 -1（無效）。

### 15.2 聊天對象名稱格式化

從即時通訊平台的訊息物件中提取對話對象的顯示名稱。

**邏輯**：
- 私人聊天：使用發送者的名字 + 姓氏，若無則使用 @使用者名稱，再無則顯示「未知」。
- 群組/頻道：使用聊天標題，若無則使用 @使用者名稱，再無則顯示聊天 ID。
- 任何異常回傳「未知」。

### 15.3 JSON 結果格式化

將工具執行的原始結果轉為可讀字串。

**邏輯**：
- null/undefined 回傳 undefined。
- 字串：嘗試解析為 JSON 並美化格式；若非 JSON 則原樣回傳。
- 物件/陣列：美化格式輸出；序列化失敗則轉為字串。

### 15.4 「連線已釋放」錯誤偵測

偵測 AI SDK 底層 JSON-RPC 層特有的「連線已釋放」錯誤。

**判斷條件**（滿足任一）：
- 錯誤碼為特定的串流已銷毀碼（ERR_STREAM_DESTROYED）
- 錯誤碼為 -32097（JSON-RPC 連線釋放碼）
- 錯誤訊息包含「pending response rejected since connection got disposed」
- 錯誤訊息包含「connection got disposed」
- 錯誤訊息包含「ERR_STREAM_DESTROYED」
- 錯誤訊息包含「Cannot call write after a stream was destroyed」

---

## 第十六章：沙箱模式

系統提供一個可選的沙箱執行模式（僅限 macOS），限制程序的檔案系統存取範圍。

**啟用條件**：
- 作業系統為 macOS。
- 環境變數設為 `1` 或 `true`。
- 尚未在沙箱環境中執行（透過內部環境變數標記，防止遞迴）。

**運作方式**：
1. 收集所有允許寫入的目錄：使用者的專案目錄（從目錄模式展開）、當前工作目錄、系統暫存目錄、AI 助理的設定目錄（包含 XDG 標準路徑）。
2. 生成沙箱策略設定檔並寫入暫存目錄。
3. 以沙箱執行器重新啟動自身，並設定標記環境變數。
4. 子程序結束後自動清理策略設定檔。

**限制策略**：
- 預設拒絕所有檔案寫入與程序執行。
- 允許寫入上述收集的目錄。
- 僅允許執行特定的系統執行檔（Node.js 執行檔、sh、env）。
- 網路存取與檔案讀取保持開放。

---

## 第十七章：可用 AI 模型

### 17.1 動態模型載入

系統的可用模型列表透過 AI 助理 SDK 動態查詢取得，而非內建於程式中。

**快取機制**：查詢結果快取 5 分鐘，避免重複呼叫 SDK。

### 17.2 預設模型選擇

預設模型的選擇邏輯：
1. 若環境變數中指定了預設模型，且該模型存在於可用列表中，使用它。
2. 否則，若列表中包含特定的標準模型名稱，使用它。
3. 若以上皆不滿足，使用列表中的第一個模型。

### 17.3 模型資訊正規化

從 SDK 取得的原始模型資料格式可能不統一。系統透過正規化處理，從多個可能的屬性名稱中提取模型名稱（依序嘗試：識別碼、名稱、模型名、模型 ID、簡稱）與提供者名稱（依序嘗試：提供者、供應商、擁有者、來源、發行者、公司）。

### 17.4 模型分組顯示

在歡迎訊息中，可用模型會依推斷的提供者名稱分組顯示。提供者名稱的推斷基於模型名稱或提供者屬性中的關鍵字比對。

---

## 第十八章：外部依賴需求摘要

本節以抽象功能需求描述系統所需的外部能力，不指定具體實作：

1. **即時通訊平台 SDK**：需要能夠以長輪詢方式接收訊息、發送訊息（含格式化與互動鍵盤）、編輯已發送訊息、處理回調查詢、下載檔案、設定訊息表情回應、查詢聊天資訊。
2. **AI 助理 SDK**：必須使用GitHub Copilot SDK 實作。要能夠建立客戶端與工作階段、發送提示詞、接收事件串流（助理回覆、工具執行、閒置通知）、銷毀/中止工作階段、查詢服務狀態與可用模型。
3. **Glob 匹配引擎**：需要能夠將 Glob 模式展開為檔案系統路徑列表。
4. **作業系統安全儲存介面**：需要能夠在 OS 鑰匙圈中讀寫字串值。
5. **密碼學工具**：需要 SHA-256 與 SHA-1 雜湊計算能力，以及密碼學安全的隨機位元組生成。
6. **TLS 工具**：需要能夠自訂伺服器身份驗證邏輯。
7. **圖片處理引擎**：需要能夠讀取圖片、自動旋轉（依 EXIF 資訊）、重新編碼為 JPEG 格式（支援品質控制與壓縮優化）。

---

## 附錄 A：狀態轉換圖（自然語言描述）

### 工作階段生命週期

```
不存在 → [使用者選擇目錄] → 閒置
閒置 → [使用者送出提示詞] → 忙碌
忙碌 → [收到閒置事件 + 佇列非空] → 忙碌（處理下一個）
忙碌 → [收到閒置事件 + 佇列為空] → 閒置
閒置/忙碌 → [使用者執行重設] → 重設中 → 閒置
閒置/忙碌 → [使用者執行關閉] → 銷毀中 → 不存在
```

### 提示詞佇列行為

```
佇列為空 + 工作階段閒置 → 提示詞直接發送
佇列為空 + 工作階段忙碌 → 提示詞加入佇列
佇列非空 + 工作階段忙碌 → 提示詞加入佇列（上限 15）
佇列已滿（15） → 拒絕新提示詞
```

---

## 附錄 B：安全架構摘要

1. **認證**：雙重 ID 驗證（聊天 ID + 使用者 ID）。
2. **機密管理**：所有機密儲存在 OS 鑰匙圈，不出現在程式碼或設定檔中。
3. **傳輸安全**：TLS + 可選的憑證鎖定。
4. **輸入過濾**：多層護欄（長度限制、內建規則、自訂策略）。
5. **輸出過濾**：敏感資訊自動遮蔽。
6. **工具輸出檢查**：偵測間接提示注入。
7. **目錄存取控制**：只允許存取設定中定義的目錄。
8. **沙箱隔離**：可選的 OS 層級檔案系統與程序執行限制。
9. **防重放**：啟動前的離線訊息被丟棄；舊的關閉指令不被執行。
10. **日誌安全**：所有日誌輸出自動遮蔽敏感資訊。

---

*本功能規格書到此結束。後續淨室重寫應以本文件為唯一依據，不得參照原始程式碼。*
